"""
The provider class implements an abstracted interface
to deploy and manage IaC executions onto a given backend.

The interface abstracts the logic of deployment from the core
code to allow implementation of new provider platforms along
the same structure within the framework.
"""